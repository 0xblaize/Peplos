# Weather (OpenWeatherMap) — leave unset to use deterministic mock weather
OPENWEATHER_API_KEY=

# LLM (Groq / Llama 3) — leave unset to skip rationale polishing and use
# the rule-based rationale text as-is
GROQ_API_KEY=

# Supabase — leave unset to use the in-memory mock closet inventory
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
